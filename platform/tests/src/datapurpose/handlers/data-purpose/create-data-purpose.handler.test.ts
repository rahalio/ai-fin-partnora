/**
 * CreateDataPurpose handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: datapurpose
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { createDataPurpose } from "@partnora/api-server/domains/datapurpose/handlers";
import type { DatapurposeDDDDependencies } from "@partnora/api-server/domains/datapurpose";
import { createDataPurpose as createDataPurposeEntity } from "@partnora/factories/datapurpose";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@partnora/tests/shared";
import { DATAPURPOSE_USE_CASES } from "../__factories__/use-cases.js";

describe("CreateDataPurpose", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: DatapurposeDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<DatapurposeDDDDependencies>(
      DATAPURPOSE_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 201 with created entity", async () => {
    const input = {} as any;
    const entity = createDataPurposeEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeCreateDataPurpose, "execute").mockResolvedValue(useCaseResult);

    await createDataPurpose(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeCreateDataPurpose.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(201);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
