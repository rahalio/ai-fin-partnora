/**
 * UpdateDeal handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: deals
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { updateDeal } from "@partnora/api-server/domains/deals/handlers";
import type { DealsDDDDependencies } from "@partnora/api-server/domains/deals";
import { createDeal as createDealEntity } from "@partnora/factories/deals";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@partnora/tests/shared";
import { DEALS_USE_CASES } from "../__factories__/use-cases.js";

describe("UpdateDeal", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: DealsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<DealsDDDDependencies>(
      DEALS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with updated entity", async () => {
    const entity = createDealEntity({ orgId });
    const input = {} as any;
    const updatedEntity = { ...entity, ...input };
    const useCaseResult = { data: updatedEntity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.params = { dealId: entity.id };
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeUpdateDeal, "execute").mockResolvedValue(useCaseResult);

    await updateDeal(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeUpdateDeal.execute).toHaveBeenCalledWith(expect.objectContaining({ dealId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
