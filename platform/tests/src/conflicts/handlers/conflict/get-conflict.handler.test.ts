/**
 * GetConflict handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: conflicts
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getConflict } from "@partnora/api-server/domains/conflicts/handlers";
import type { ConflictsDDDDependencies } from "@partnora/api-server/domains/conflicts";
import { createConflict as createConflictEntity } from "@partnora/factories/conflicts";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@partnora/tests/shared";
import { CONFLICTS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetConflict", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ConflictsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ConflictsDDDDependencies>(
      CONFLICTS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createConflictEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetConflict, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { conflictId: entity.id };

    await getConflict(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetConflict.execute).toHaveBeenCalledWith(expect.objectContaining({ conflictId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { conflictId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetConflict, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getConflict(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
