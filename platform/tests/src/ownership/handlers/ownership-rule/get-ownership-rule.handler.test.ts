/**
 * GetOwnershipRule handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: ownership
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getOwnershipRule } from "@partnora/api-server/domains/ownership/handlers";
import type { OwnershipDDDDependencies } from "@partnora/api-server/domains/ownership";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@partnora/tests/shared";
import { OWNERSHIP_USE_CASES } from "../__factories__/use-cases.js";

describe("GetOwnershipRule", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: OwnershipDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<OwnershipDDDDependencies>(
      OWNERSHIP_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetOwnershipRule, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { ownershipRuleId: entity.id };

    await getOwnershipRule(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetOwnershipRule.execute).toHaveBeenCalledWith(expect.objectContaining({ ownershipRuleId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { ownershipRuleId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetOwnershipRule, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getOwnershipRule(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
