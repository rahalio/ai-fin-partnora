/**
 * Partner Entity Factory
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: partners
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { faker } from "@faker-js/faker";
import type { PartnerEntity } from "@partnora/core/partners/models/index.js";
import { partnersSchemas } from "@partnora/core/partners";
import { ulid } from "ulid";

// Domain prefix from OpenAPI spec info.x-domain
const DOMAIN_PREFIX = "ptn";

export function createPartner(overrides: Partial<PartnerEntity> = {}) {
  const raw = {
    id: overrides.id ?? `${DOMAIN_PREFIX}_${ulid().toLowerCase()}`,
    createdAt: overrides.createdAt ?? new Date().toISOString(),
    updatedAt: overrides.updatedAt ?? new Date().toISOString(),
    name: overrides.name ?? faker.person.fullName(),
    partnerId: overrides.partnerId ?? `ptn_${ulid().toLowerCase()}`,
    posture: overrides.posture ?? "bank",
    ...overrides,
  };

  return partnersSchemas.Partner.parse(raw);
}

export function partnerBatch(n = 3, overrides = {}) {
  return Array.from({ length: n }, () => createPartner(overrides));
}
