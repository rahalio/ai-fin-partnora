/**
 * UpdatePartner handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: partners
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { updatePartner } from "@partnora/api-server/domains/partners/handlers";
import type { PartnersDDDDependencies } from "@partnora/api-server/domains/partners";
import { createPartner as createPartnerEntity } from "@partnora/factories/partners";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@partnora/tests/shared";
import { PARTNERS_USE_CASES } from "../__factories__/use-cases.js";

describe("UpdatePartner", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: PartnersDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<PartnersDDDDependencies>(
      PARTNERS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with updated entity", async () => {
    const entity = createPartnerEntity({ orgId });
    const input = {} as any;
    const updatedEntity = { ...entity, ...input };
    const useCaseResult = { data: updatedEntity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.params = { partnerId: entity.id };
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeUpdatePartner, "execute").mockResolvedValue(useCaseResult);

    await updatePartner(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeUpdatePartner.execute).toHaveBeenCalledWith(expect.objectContaining({ partnerId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
