/**
 * GetChannelNode handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: channels
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getChannelNode } from "@partnora/api-server/domains/channels/handlers";
import type { ChannelsDDDDependencies } from "@partnora/api-server/domains/channels";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@partnora/tests/shared";
import { CHANNELS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetChannelNode", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ChannelsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ChannelsDDDDependencies>(
      CHANNELS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetChannelNode, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { channelNodeId: entity.id };

    await getChannelNode(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetChannelNode.execute).toHaveBeenCalledWith(expect.objectContaining({ channelNodeId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { channelNodeId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetChannelNode, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getChannelNode(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
