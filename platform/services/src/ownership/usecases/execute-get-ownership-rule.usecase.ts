/**
 * ExecuteGetOwnershipRule - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: ownership
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetOwnershipRule Use Case
 *
 * DDD: Application use case for get OwnershipRule.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetOwnershipRuleInput, GetOwnershipRuleOutput } from "../dto/ownership-rule.dto";
import type { ExecutionContextService, IdGeneratorService } from "@partnora/services/_shared/index.js";
import type { OwnershipRuleRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetOwnershipRule {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly ownershipRule: OwnershipRuleRepository
  ) {}

  async execute(input: GetOwnershipRuleInput): Promise<GetOwnershipRuleOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.ownId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).ownershipRuleId) {
      throw new ValidationError("ownershipRuleId is required");
    }
    const ownershipruleId = (input as any).ownershipRuleId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.ownershipRule.getOwnershipRule({ ownershipRuleId: ownershipruleId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${ownershipruleId}`);
    }

    return entity as GetOwnershipRuleOutput;
  }
}
