/**
 * ExecuteIngestChannelMetrics - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: channels
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteIngestChannelMetrics Use Case
 *
 * DDD: Application use case for get Metric.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { IngestChannelMetricsInput, IngestChannelMetricsOutput } from "../dto/metric.dto";
import type { ExecutionContextService, IdGeneratorService } from "@partnora/services/_shared/index.js";
import type { MetricRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteIngestChannelMetrics {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly metric: MetricRepository
  ) {}

  async execute(input: IngestChannelMetricsInput): Promise<IngestChannelMetricsOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.chnId();

    // All field names extracted from OpenAPI spec: activations, asOf, channelNodeId, dormancyRate
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).channelNodeId) {
      throw new ValidationError("channelNodeId is required");
    }
    const channelnodeId = (input as any).channelNodeId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.metric.ingestChannelMetrics({ channelNodeId: channelnodeId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${channelnodeId}`);
    }

    return entity as IngestChannelMetricsOutput;
  }
}
