/**
 * ExecuteAcceptConflictMitigation - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: conflicts
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteAcceptConflictMitigation Use Case
 *
 * DDD: Application use case for get Accept.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { AcceptConflictMitigationInput, AcceptConflictMitigationOutput } from "../dto/accept.dto";
import type { ExecutionContextService, IdGeneratorService } from "@partnora/services/_shared/index.js";
import type { AcceptRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteAcceptConflictMitigation {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly accept: AcceptRepository
  ) {}

  async execute(input: AcceptConflictMitigationInput): Promise<AcceptConflictMitigationOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.cflId();

    // All field names extracted from OpenAPI spec: notes
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).conflictId) {
      throw new ValidationError("conflictId is required");
    }
    const conflictId = (input as any).conflictId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.accept.acceptConflictMitigation({ conflictId: conflictId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${conflictId}`);
    }

    return entity as AcceptConflictMitigationOutput;
  }
}
