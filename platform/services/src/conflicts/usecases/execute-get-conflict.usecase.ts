/**
 * ExecuteGetConflict - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: conflicts
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetConflict Use Case
 *
 * DDD: Application use case for get Conflict.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetConflictInput, GetConflictOutput } from "../dto/conflict.dto";
import type { ExecutionContextService, IdGeneratorService } from "@partnora/services/_shared/index.js";
import type { ConflictRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetConflict {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly conflict: ConflictRepository
  ) {}

  async execute(input: GetConflictInput): Promise<GetConflictOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.cflId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).conflictId) {
      throw new ValidationError("conflictId is required");
    }
    const conflictId = (input as any).conflictId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.conflict.getConflict({ conflictId: conflictId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${conflictId}`);
    }

    return entity as GetConflictOutput;
  }
}
