/**
 * ExecuteGetPilot - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: pilots
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetPilot Use Case
 *
 * DDD: Application use case for get Pilot.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetPilotInput, GetPilotOutput } from "../dto/pilot.dto";
import type { ExecutionContextService, IdGeneratorService } from "@partnora/services/_shared/index.js";
import type { PilotRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetPilot {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly pilot: PilotRepository
  ) {}

  async execute(input: GetPilotInput): Promise<GetPilotOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.pltId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).pilotId) {
      throw new ValidationError("pilotId is required");
    }
    const pilotId = (input as any).pilotId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.pilot.getPilot({ pilotId: pilotId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${pilotId}`);
    }

    return entity as GetPilotOutput;
  }
}
