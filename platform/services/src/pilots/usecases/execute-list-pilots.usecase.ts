/**
 * ExecuteListPilots - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: pilots
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteListPilots Use Case
 *
 * DDD: Application use case for list Pilot.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { ListPilotsInput, ListPilotsOutput } from "../dto/pilot.dto";
import type { ExecutionContextService, IdGeneratorService } from "@partnora/services/_shared/index.js";
import type { PilotRepository } from "../ports";

export class ExecuteListPilots {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly pilot: PilotRepository
  ) {}

  async execute(input: ListPilotsInput): Promise<ListPilotsOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.pltId();

    // All field names extracted from OpenAPI spec: none
    // List entities via repository
    const result = await this.pilot.listPilots({ ...input, orgId: this.context.getOrgId(), correlationId } as any);

    return result as ListPilotsOutput;
  }
}
