/**
 * ExecuteListDeals - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: deals
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteListDeals Use Case
 *
 * DDD: Application use case for list Deal.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { ListDealsInput, ListDealsOutput } from "../dto/deal.dto";
import type { ExecutionContextService, IdGeneratorService } from "@partnora/services/_shared/index.js";
import type { DealRepository } from "../ports";

export class ExecuteListDeals {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly deal: DealRepository
  ) {}

  async execute(input: ListDealsInput): Promise<ListDealsOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.dealId();

    // All field names extracted from OpenAPI spec: none
    // List entities via repository
    const result = await this.deal.listDeals({ ...input, orgId: this.context.getOrgId(), correlationId } as any);

    return result as ListDealsOutput;
  }
}
