/**
 * DealRepositoryAdapter - Port Adapter Implementation
 *
 * Generated by Port Adapter Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: deals
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * DealRepositoryAdapter
 *
 * DDD: Infrastructure adapter that implements DealRepository port.
 * Handles infrastructure concerns (HTTP, database, external APIs).
 * Encapsulates Ddb creation - api-server passes dynamoClient only.
 */

import type {
  DealRepository,
} from "@partnora/services/deals";
import type { AdapterDynamoDBClient } from "../_shared/dynamodb-client-types.js";
import { DealRepositoryDdb } from "./deal-repository.ddb.js";

/**
 * DealRepositoryAdapter
 *
 * Implements DealRepository port for infrastructure layer.
 */
export class DealRepositoryAdapter implements DealRepository {
  private readonly ddb: DealRepositoryDdb;

  constructor(
    private readonly dynamoClient: AdapterDynamoDBClient
  ) {
    this.ddb = new DealRepositoryDdb(this.dynamoClient);
  }

  async listDeals(input: Parameters<DealRepository['listDeals']>[0]): Promise<Awaited<ReturnType<DealRepository['listDeals']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.listDeals(input);
  }

  async createDeal(input: Parameters<DealRepository['createDeal']>[0]): Promise<Awaited<ReturnType<DealRepository['createDeal']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.createDeal(input);
  }

  async getDeal(input: Parameters<DealRepository['getDeal']>[0]): Promise<Awaited<ReturnType<DealRepository['getDeal']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.getDeal(input);
  }

  async updateDeal(input: Parameters<DealRepository['updateDeal']>[0]): Promise<Awaited<ReturnType<DealRepository['updateDeal']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.updateDeal(input);
  }
}
