/**
 * GateRepositoryAdapter - Port Adapter Implementation
 *
 * Generated by Port Adapter Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: gates
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * GateRepositoryAdapter
 *
 * DDD: Infrastructure adapter that implements GateRepository port.
 * Handles infrastructure concerns (HTTP, database, external APIs).
 * Encapsulates Ddb creation - api-server passes dynamoClient only.
 */

import type {
  GateRepository,
} from "@partnora/services/gates";
import type { AdapterDynamoDBClient } from "../_shared/dynamodb-client-types.js";
import { GateRepositoryDdb } from "./gate-repository.ddb.js";

/**
 * GateRepositoryAdapter
 *
 * Implements GateRepository port for infrastructure layer.
 */
export class GateRepositoryAdapter implements GateRepository {
  private readonly ddb: GateRepositoryDdb;

  constructor(
    private readonly dynamoClient: AdapterDynamoDBClient
  ) {
    this.ddb = new GateRepositoryDdb(this.dynamoClient);
  }

  async listDealGates(input: Parameters<GateRepository['listDealGates']>[0]): Promise<Awaited<ReturnType<GateRepository['listDealGates']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.listDealGates(input);
  }

  async submitGateEvidence(input: Parameters<GateRepository['submitGateEvidence']>[0]): Promise<Awaited<ReturnType<GateRepository['submitGateEvidence']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.submitGateEvidence(input);
  }

  async decideGate(input: Parameters<GateRepository['decideGate']>[0]): Promise<Awaited<ReturnType<GateRepository['decideGate']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.decideGate(input);
  }
}
