/**
 * PartnerRepositoryAdapter - Port Adapter Implementation
 *
 * Generated by Port Adapter Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: partners
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * PartnerRepositoryAdapter
 *
 * DDD: Infrastructure adapter that implements PartnerRepository port.
 * Handles infrastructure concerns (HTTP, database, external APIs).
 * Encapsulates Ddb creation - api-server passes dynamoClient only.
 */

import type {
  PartnerRepository,
} from "@partnora/services/partners";
import type { AdapterDynamoDBClient } from "../_shared/dynamodb-client-types.js";
import { PartnerRepositoryDdb } from "./partner-repository.ddb.js";

/**
 * PartnerRepositoryAdapter
 *
 * Implements PartnerRepository port for infrastructure layer.
 */
export class PartnerRepositoryAdapter implements PartnerRepository {
  private readonly ddb: PartnerRepositoryDdb;

  constructor(
    private readonly dynamoClient: AdapterDynamoDBClient
  ) {
    this.ddb = new PartnerRepositoryDdb(this.dynamoClient);
  }

  async listPartners(input: Parameters<PartnerRepository['listPartners']>[0]): Promise<Awaited<ReturnType<PartnerRepository['listPartners']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.listPartners(input);
  }

  async createPartner(input: Parameters<PartnerRepository['createPartner']>[0]): Promise<Awaited<ReturnType<PartnerRepository['createPartner']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.createPartner(input);
  }

  async getPartner(input: Parameters<PartnerRepository['getPartner']>[0]): Promise<Awaited<ReturnType<PartnerRepository['getPartner']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.getPartner(input);
  }

  async updatePartner(input: Parameters<PartnerRepository['updatePartner']>[0]): Promise<Awaited<ReturnType<PartnerRepository['updatePartner']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.updatePartner(input);
  }
}
