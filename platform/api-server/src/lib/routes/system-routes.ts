/**
 * System Routes - Health check and routes listing
 *
 * AUTO-GENERATED by codegen (api_server_system_routes). Do not edit by hand.
 * Domains and routeExamples are derived from discovered api-server domains and
 * each domain's OpenAPI bundled spec.
 */

import type { FastifyInstance } from "fastify";
import {
  getCoreTableName,
  getBaseTableName,
  getAnalyticsTableName,
} from "@partnora/adapters";
import { getRegisteredRoutes } from "./route-registry.js";

/**
 * Register system routes (health check, routes listing)
 */
export function registerSystemRoutes(fastify: FastifyInstance): void {
  // Health check (public - no auth required)
  fastify.get("/health", { config: { scope: "public" } }, async () => {
    try {
      const coreTable = getCoreTableName();
      const baseTable = getBaseTableName();
      const analyticsTable = getAnalyticsTableName();
      return {
        status: "ok",
        database: "dynamodb",
        tables: {
          core: coreTable,
          base: baseTable,
          analytics: analyticsTable,
        },
      };
    } catch (error: unknown) {
      const errorMessage =
        error instanceof Error ? error.message : String(error);
      fastify.log.warn({ error: errorMessage }, "Health check failed");
      return { status: "error", database: "disconnected", error: errorMessage };
    }
  });

  // Routes listing endpoint (public)
  fastify.get("/routes", { config: { scope: "public" } }, async () => {
    const domains = [
      "audits", "channels", "conflicts", "datapurpose", "deals", "economics", "gates", "identity", "opportunities", "ownership", "partners", "pilots"
    ];

    const routeExamples: Record<
      string,
      Array<{ method: string; path: string; description?: string }>
    > = {
      audits: [{ method: "GET", path: "/v1/audits/exports" }, { method: "POST", path: "/v1/audits/exports" }, { method: "GET", path: "/v1/audits/exports/:auditExportId" }, { method: "GET", path: "/v1/incidents" }, { method: "POST", path: "/v1/incidents" }],
      channels: [{ method: "GET", path: "/v1/channels" }, { method: "POST", path: "/v1/channels" }, { method: "GET", path: "/v1/channels/:channelNodeId" }, { method: "POST", path: "/v1/channels/metrics" }],
      conflicts: [{ method: "GET", path: "/v1/conflicts" }, { method: "POST", path: "/v1/conflicts" }, { method: "GET", path: "/v1/conflicts/:conflictId" }, { method: "POST", path: "/v1/conflicts/:conflictId/accept" }],
      datapurpose: [{ method: "GET", path: "/v1/data-purposes" }, { method: "POST", path: "/v1/data-purposes" }, { method: "GET", path: "/v1/data-purposes/:dataPurposeId" }, { method: "DELETE", path: "/v1/data-purposes/:dataPurposeId" }, { method: "POST", path: "/v1/data-purposes/export-check" }],
      deals: [{ method: "GET", path: "/v1/deals" }, { method: "POST", path: "/v1/deals" }, { method: "GET", path: "/v1/deals/:dealId" }, { method: "PATCH", path: "/v1/deals/:dealId" }, { method: "POST", path: "/v1/deals/:dealId/submit-gates" }, { method: "POST", path: "/v1/deals/:dealId/launch" }, { method: "POST", path: "/v1/deals/:dealId/remediate" }, { method: "POST", path: "/v1/deals/:dealId/exit" }, { method: "POST", path: "/v1/deals/ma-reassessments" }],
      economics: [{ method: "GET", path: "/v1/economics" }, { method: "POST", path: "/v1/economics" }, { method: "GET", path: "/v1/economics/:economicsSnapshotId" }],
      gates: [{ method: "GET", path: "/v1/deals/:dealId/gates" }, { method: "POST", path: "/v1/deals/:dealId/gates" }, { method: "POST", path: "/v1/deals/:dealId/gates/:gateId" }],
      identity: [{ method: "GET", path: "/v0/tenants/me/api-keys" }, { method: "POST", path: "/v0/tenants/me/api-keys" }, { method: "GET", path: "/v0/tenants/me/api-keys/:keyId" }, { method: "DELETE", path: "/v0/tenants/me/api-keys/:keyId" }, { method: "GET", path: "/v0/tenants/me/users" }, { method: "POST", path: "/v0/tenants/me/users" }, { method: "GET", path: "/v0/tenants/me/users/:userId" }, { method: "PATCH", path: "/v0/tenants/me/users/:userId" }, { method: "POST", path: "/v0/tenants/me/users/:userId/disable" }, { method: "POST", path: "/v0/tenants/me/users/:userId/enable" }, { method: "POST", path: "/v0/auth/login" }, { method: "GET", path: "/v0/auth/me" }, { method: "PATCH", path: "/v0/auth/me" }, { method: "POST", path: "/v0/auth/refresh" }, { method: "POST", path: "/v0/auth/logout" }],
      opportunities: [{ method: "GET", path: "/v1/opportunities" }, { method: "POST", path: "/v1/opportunities" }, { method: "GET", path: "/v1/opportunities/:opportunityId" }, { method: "POST", path: "/v1/opportunities/:opportunityId/score" }, { method: "POST", path: "/v1/opportunities/:opportunityId/advance" }],
      ownership: [{ method: "GET", path: "/v1/ownership-rules" }, { method: "POST", path: "/v1/ownership-rules" }, { method: "GET", path: "/v1/ownership-rules/:ownershipRuleId" }, { method: "PATCH", path: "/v1/ownership-rules/:ownershipRuleId" }],
      partners: [{ method: "GET", path: "/v1/partners" }, { method: "POST", path: "/v1/partners" }, { method: "GET", path: "/v1/partners/:partnerId" }, { method: "PATCH", path: "/v1/partners/:partnerId" }],
      pilots: [{ method: "GET", path: "/v1/pilots" }, { method: "POST", path: "/v1/pilots" }, { method: "GET", path: "/v1/pilots/:pilotId" }, { method: "POST", path: "/v1/pilots/:pilotId/attach-parent" }, { method: "POST", path: "/v1/pilots/:pilotId/quarantine" }],
    };

    return {
      baseUrl: "http://localhost:4000",
      routePattern: "/orgs/:orgId/{domain}/{resource}",
      authentication:
        "All routes (except /health and /routes) require JWT token in Authorization header",
      domains: domains.sort(),
      routeExamples,
      systemRoutes: [
        { method: "GET", path: "/health", description: "Health check endpoint" },
        { method: "GET", path: "/routes", description: "List available routes" },
      ],
      note: "Replace :orgId with actual organization ID. All domain routes require authentication.",
    };
  });

  // Full route list for API tester page (public)
  fastify.get("/routes/full", { config: { scope: "public" } }, async () => {
    const routes = getRegisteredRoutes();
    const byDomain: Record<
      string,
      Array<{ method: string; path: string }>
    > = {};
    const system: Array<{ method: string; path: string }> = [];
    for (const r of routes) {
      const m = r.path.match(/^\/orgs\/:orgId\/([^/]+)/);
      if (m) {
        const domain = m[1];
        if (!byDomain[domain]) byDomain[domain] = [];
        byDomain[domain].push(r);
      } else {
        system.push(r);
      }
    }
    return {
      baseUrl: "http://localhost:4000",
      total: routes.length,
      system,
      byDomain: Object.keys(byDomain)
        .sort()
        .reduce(
          (acc, k) => ({ ...acc, [k]: byDomain[k] }),
          {} as Record<string, Array<{ method: string; path: string }>>,
        ),
    };
  });
}
